# Example results

Output from a full dry run of the artifact pipeline (ledger responses
simulated, payload store and all Node tooling real). Use these to see the
expected shape of every artifact before your first live run, and to draft the
Гл. 5 text while the Fabric environment is still being set up.

After `./run-all.sh` completes, `results/` holds the equivalents produced by
the actual chaincode — those are the ones cited in the paper.
